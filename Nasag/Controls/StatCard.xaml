<UserControl x:Class="Nasag.Controls.StatCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="Root">
    <Border Style="{StaticResource CardBorder}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Label, ElementName=Root}"
                           Style="{StaticResource CaptionTextStyle}"
                           Margin="0,0,0,8" />
                <TextBlock Text="{Binding Value, ElementName=Root}"
                           Style="{StaticResource LargeNumberTextStyle}" />
                <TextBlock Text="{Binding Hint, ElementName=Root}"
                           Style="{StaticResource CaptionTextStyle}"
                           Margin="0,6,0,0" />
            </StackPanel>

            <Border Grid.Column="1"
                    Width="48" Height="48"
                    CornerRadius="12"
                    VerticalAlignment="Top"
                    Background="{Binding AccentBrush, ElementName=Root}">
                <Path Data="{Binding IconGeometry, ElementName=Root}"
                      Stroke="{Binding IconBrush, ElementName=Root}"
                      StrokeThickness="1.7"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      StrokeLineJoin="Round"
                      Fill="Transparent"
                      Stretch="Uniform"
                      Width="22" Height="22" />
            </Border>
        </Grid>
    </Border>
</UserControl>
