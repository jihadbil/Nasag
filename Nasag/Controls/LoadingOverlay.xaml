<UserControl x:Class="Nasag.Controls.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="Root"
             Visibility="Collapsed"
             IsHitTestVisible="True">
    <Grid Background="#B3FFFFFF">
        <Grid.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                    <Storyboard RepeatBehavior="Forever">
                        <DoubleAnimation Storyboard.TargetName="Spinner"
                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                         From="0" To="360" Duration="0:0:1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid x:Name="Spinner" Width="42" Height="42" RenderTransformOrigin="0.5,0.5">
                <Grid.RenderTransform>
                    <RotateTransform Angle="0" />
                </Grid.RenderTransform>
                <Ellipse Stroke="{StaticResource BorderBrush}" StrokeThickness="4" />
                <Path Stroke="{StaticResource TealPrimaryBrush}"
                      StrokeThickness="4"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      Data="M 21 2 A 19 19 0 0 1 40 21" />
            </Grid>
            <TextBlock Text="{Binding Message, ElementName=Root}"
                       Style="{StaticResource MutedTextStyle}"
                       FontWeight="Medium"
                       HorizontalAlignment="Center"
                       Margin="0,14,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
